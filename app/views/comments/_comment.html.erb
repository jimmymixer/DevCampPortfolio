<div class="comment-card">
  <div class="card">
    <div class="card-block">
      <div class="row comment-padding">

        <div class="col-md-2">
          <%= gravatar_helper comment.user %>
        </div>

        <div class="col-md-10">
          <div>
            <!-- This value is coming from jobs/comment_broadcast_jobs.rb -->
            <%= comment.content %>
          </div>
          <div class="comment-attribution">
            Comment posted by <span class="comment-name"><%= comment.user.first_name %></span>, <%= distance_of_time_in_words(comment.created_at, Time.now) %> ago
          </div>
        </div>

      </div>
    </div>
  </div>
</div>
